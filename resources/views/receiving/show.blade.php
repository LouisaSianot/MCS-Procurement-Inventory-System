<x-app-layout title="Receipt {{ $receipt->receipt_number }}">
    <x-page-header title="Receipt {{ $receipt->receipt_number }}" description="Posted purchase receipt."><x-slot name="actions"><a href="{{ route('receiving.index') }}" class="btn btn-secondary">Back to Receipts</a></x-slot></x-page-header>
    <div class="grid grid-cols-1 gap-6 lg:grid-cols-3"><section class="card lg:col-span-2"><div class="border-b border-slate-200 p-5"><h2 class="text-base font-semibold">Receipt details</h2></div><dl class="grid grid-cols-1 gap-5 p-5 sm:grid-cols-2"><div><dt class="text-xs uppercase text-slate-400">Purchase Order</dt><dd class="mt-1 font-mono font-semibold">{{ $receipt->purchaseOrder->po_number }}</dd></div><div><dt class="text-xs uppercase text-slate-400">Supplier</dt><dd class="mt-1">{{ $receipt->purchaseOrder->supplier->name }}</dd></div><div><dt class="text-xs uppercase text-slate-400">Received date</dt><dd class="mt-1">{{ $receipt->received_at->format('d M Y') }}</dd></div><div><dt class="text-xs uppercase text-slate-400">Received by</dt><dd class="mt-1">{{ $receipt->receiver->name }}</dd></div></dl></section><section class="card"><div class="border-b border-slate-200 p-5"><h2 class="text-base font-semibold">PO status</h2></div><div class="p-5"><x-status-badge :status="$receipt->purchaseOrder->status" /></div></section></div>
    <section class="card mt-6"><div class="border-b border-slate-200 p-5"><h2 class="text-base font-semibold">Received items</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Item</th><th class="text-right">Quantity</th><th class="text-right">Unit cost</th><th>Inventory result</th></tr></thead><tbody>@foreach ($receipt->items as $line)<tr><td>{{ $line->purchaseOrderItem->description }}</td><td class="text-right">{{ $line->quantity_received }}</td><td class="text-right">K {{ number_format((float) $line->unit_cost, 2) }}</td><td>@if($line->inventoryMovement)<span class="text-emerald-700">Stock updated to {{ $line->inventoryMovement->stock_after }}</span>@else<span class="text-slate-500">Non-stock: no inventory movement</span>@endif</td></tr>@endforeach</tbody></table></div></section>
</x-app-layout>
