<x-app-layout title="User Management"><x-page-header title="Users" description="Assign each account to a branch, role, and active status."><x-slot name="actions"><a href="{{ route('admin.roles.index') }}" class="btn btn-secondary">Manage roles</a><a href="{{ route('admin.users.create') }}" class="btn btn-primary" aria-label="Add user" title="Add user"><i data-lucide="user-plus" class="h-4 w-4"></i></a></x-slot></x-page-header><section class="card overflow-hidden"><form class="border-b border-slate-200 p-4"><div class="flex gap-3"><input class="input max-w-md" name="search" value="{{ $search }}" placeholder="Search user name or email"><button class="btn btn-secondary">Search</button></div></form><div class="table-wrap"><table class="data-table"><thead><tr><th>User ID</th><th>Name</th><th>Email / login</th><th>Branch</th><th>Role</th><th>Status</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($users as $user)<tr><td class="font-mono text-xs">#{{ $user->id }}</td><td class="font-semibold text-slate-900">{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ $user->branch?->name ?: '—' }}</td><td>{{ $user->getRoleNames()->join(', ') ?: '—' }}</td><td><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $user->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $user->is_active ? 'Active' : 'Inactive' }}</span></td><td><div class="flex justify-end gap-2"><a class="btn btn-ghost !px-2.5 !py-1.5" href="{{ route('admin.users.edit', $user) }}">Edit</a>@if(!$user->is(auth()->user()))<form method="POST" action="{{ route('admin.users.destroy', $user) }}" onsubmit="return confirm('Delete this user account?');">@csrf @method('DELETE')<button class="btn btn-ghost !px-2.5 !py-1.5 text-rose-600">Delete</button></form>@endif</div></td></tr>@empty<tr><td colspan="7" class="py-10 text-center text-slate-500">No users found.</td></tr>@endforelse</tbody></table></div>@if($users->hasPages())<div class="border-t border-slate-200 px-4 py-3">{{ $users->links() }}</div>@endif</section></x-app-layout>
