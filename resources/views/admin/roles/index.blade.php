<x-app-layout title="Roles & Permissions"><x-page-header title="Roles & permissions" description="Configure permissions for the four roles supported by Project Specification v4."><x-slot name="actions"><a href="{{ route('admin.users.index') }}" class="btn btn-secondary">Back to users</a></x-slot></x-page-header><div class="grid gap-5 lg:grid-cols-2">@foreach($roles as $role)<form method="POST" action="{{ route('admin.roles.update', $role) }}" class="card p-5">@csrf @method('PUT')<h3 class="text-base font-semibold text-slate-900">{{ $role->name }}</h3><p class="mt-1 text-sm text-slate-500">Choose the actions this role may perform.</p><div class="mt-4 space-y-3">@foreach($permissions as $permission)<label class="flex items-center gap-3 rounded-lg border border-slate-200 px-3 py-2.5 text-sm text-slate-700"><input type="checkbox" name="permissions[]" value="{{ $permission->name }}" @checked($role->hasPermissionTo($permission)) class="rounded border-slate-300 text-brand-600 focus:ring-brand-500"><span>{{ $permission->name }}</span></label>@endforeach</div><div class="mt-5 flex justify-end"><button class="btn btn-primary">Save permissions</button></div></form>@endforeach</div></x-app-layout>
